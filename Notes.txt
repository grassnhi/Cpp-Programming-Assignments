Pass by Reference

1/
    void swap( int *a , int *b ) // * here and below
    {
        int temp = *a;
        *a = *b;
        *b = temp;
    } // use poiter

2/
    void squareByReference(int &cRef ) //& here
    {
        cRef *= cRef;   
    }
